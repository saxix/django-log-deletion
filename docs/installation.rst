============
Installation
============

At the command line::

    $ easy_install django-log-deletion

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-log-deletion
    $ pip install django-log-deletion
